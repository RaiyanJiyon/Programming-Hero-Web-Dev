const numbers = [1, 2, 3, 4, 5];

// forEach method don't return anything. we have to write console.log to see the result in the forEach method.
const result = numbers.forEach(number => console.log(number * 2));
